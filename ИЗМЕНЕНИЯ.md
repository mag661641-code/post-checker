# Журнал изменений сервиса «Проверка постов»

Документ ведётся по датам. Новые изменения добавляются сверху.
Правки делаются через Claude Code, затем публикуются на GitHub и подтягиваются
в Streamlit Cloud (при необходимости — кнопка **Manage app → Reboot app**).

---

## 12 сентября 2026

### Раскладка «Проверка постов»: просторнее (коммит 7de18a5)
- Список постов и карточка в пропорции 5:6, список стал шире.
- Компактная таблица списка: **Дата · Бренд · Замечания · Пост** (счётчики
  замечаний одной колонкой `🔴 2 🟡 1`, «✅» если чисто); высота фиксированная.
- Над списком в одну строку: сортировка, счётчик постов, «Орфография во всех».
- Карточка компактнее: мета-строка и кнопки «Открыть в таблице» / «Копировать»
  в один ряд, замечания без громоздких рамок, фото рядами по 4.
- Контакты бренда **не** проверяются у типа «Развлекательный» и в канале для
  сотрудников (напр. `t.me/SMUdaily`).

### Проверка публикаций: все площадки + честные статусы (коммит 09f45df)
- Статусы ссылок вместо ложного «совпадает»: ✅ текст совпал · ⚠️ отличается ·
  🔗 открывается, текст не сверялся · ❌ не найден · 🔒 проверьте вручную ·
  ➖ нет ссылки · 👁 проверено вручную.
- Поддержаны площадки: Telegram, ВКонтакте, Одноклассники, Max, WhatsApp, Дзен;
  распознаётся канал другого бренда.
- Фильтр «Площадка», два вида результата «По постам» / «Все ссылки», строка-итог
  со счётчиками, легенда, кнопка «Отметить: проверено вручную».
- Кнопка запуска считает правильно: «1 пост, 5 ссылок»; «Остановить» рядом и
  только во время проверки.

### Единый выбор месяца и года (коммит 09f45df)
- Общий блок `[←] [Месяц ▾] [Год ▾] [→]` на экранах «Проверка постов»,
  «Проверка публикаций», «Обязательные праздники».
- Количество постов — отдельной строкой под выбором; переход через год
  (декабрь → январь). Числа склоняются («1 пост», «2 поста», «5 постов»).

### Контакты по бренду × типу поста + сноска, тире, фото (коммит 886d16a)
- Что проверять в посте, зависит от **типа и бренда** (телефон / сайт / e-mail /
  хэштеги). Значения по умолчанию — в настройках, «Типы постов»; отличия
  бренда — в карточке бренда, «Правила проверки по типам».
- Сайт засчитывается и **ссылкой-анкором** («на нашем сайте»): анкор-ссылки
  читаются из Google-таблицы (Sheets API) и из Excel (openpyxl).
- Смягчены тексты замечаний про сайт для информационных постов.
- Сноска про цену и оферту у «Спецпредложения» (тексты по 5 брендам) — 🟡.
- Фото отгрузок: нужны обе строки «ЛОГО:» и «БЕЗ ЛОГО:».
- Тексты сносок и правила по типам занесены для СМУ, ИМП, МПЭ, МПИ, АПС.

### Исправление загрузчика и ложных ошибок (в работе, ещё не на GitHub)
- **Даты и группировка постов.** Убрана причина «102 поста без даты»: строки
  «Подпись:/Клиент:» и строки только с соцсетью больше не считаются отдельными
  постами, а присоединяются к своему посту. Новый пост начинается со строки с
  датой (или со строки с текстом и соцсетью без даты). Колонка даты ищется по
  списку названий, включая «Когда выложить» (лист СМУ). Проверка на эталонном
  файле: ИМП 100 / МПЭ 100 / МПИ 43 / АПС 42 — все с датами; на СМУ настоящих
  постов без даты — 3.
- **Дата с примечанием.** Значения вида «18.07.2025 (доп)» или «30.06.2025 /
  ЯБ 01.07.2025» распознаются: берётся первая дата, остаток показывается в
  карточке как «в таблице указано: …», ошибка не выдаётся.
- **«Тип поста не распознан».** Устранена ложная ошибка для «Информационный»:
  распознавание типов устойчиво к отсутствию настроек (подмешиваются встроенные
  значения по умолчанию), сравнение — без учёта регистра, пробелов и переносов.
- **Короткое тире «–».** Проверка переведена в уровень 🔵 «Совет» и по умолчанию
  выключена; формулировка нейтральная. Включается в настройках.

### Запланировано следующим шагом (UI-редизайн «Проверка постов»)
- Убрать фильтр «Исполнитель» (и раздел «Исполнители» из настроек).
- Аккуратная раскладка фильтров ровными строками, фильтры под сворачиваемым
  блоком «Фильтры».
- Посты — одним списком на всю ширину, каждый пост в раскрывающемся блоке
  (без узких колонок и обрезки текста); «Развернуть все» / «Свернуть все».
- Отдельный блок внизу «⚠️ Посты без даты» с раскрывающимися постами.
- Мягкое, аккуратное оформление без лишних «стикеров».

### Прочее за день
- Добавлен пароль на вход в сервис (задаётся в секретах Streamlit, блок `[auth]`).
- Заведён этот журнал изменений.
